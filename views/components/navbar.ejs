<!-- Inline script to prevent sidebar flash - runs immediately -->
<script>
(function() {
    // Apply saved state immediately before render
    if (localStorage.getItem('sidebarMinimized') === 'true') {
        document.documentElement.classList.add('sidebar-minimized-early');
    }
})();
</script>

<!-- Fixed Sidebar -->
<div class="fixed-sidebar" id="sidebar">
    <div class="sidebar-header">
        <div class="sidebar-logo">
            <img src="/public/img/logo-smata.png" alt="Logo" class="logo-img">
            <span class="logo-text">TUKIN SMATAJAYA</span>
        </div>
    </div>
    
    <div class="sidebar-content">
        <ul class="sidebar-nav">
            <li class="sidebar-item <%= req === '/' ? 'active' : '' %>">
                <a class="sidebar-link" href="/">
                    <div class="nav-icon">
                        <i class="bi bi-speedometer2"></i>
                    </div>
                    <span class="nav-text">Dashboard</span>
                </a>
            </li>
            <li class="sidebar-item <%= req === '/staff' ? 'active' : '' %>">
                <a class="sidebar-link" href="/staff">
                    <div class="nav-icon">
                        <i class="bi bi-people-fill"></i>
                    </div>
                    <span class="nav-text">Karyawan & Penilaian</span>
                </a>
            </li>
            <li class="sidebar-item <%= req === '/periode' ? 'active' : '' %>">
                <a class="sidebar-link" href="/periode">
                    <div class="nav-icon">
                        <i class="bi bi-calendar-event"></i>
                    </div>
                    <span class="nav-text">Kategori Instrumen</span>
                </a>
            </li>
            <li class="sidebar-item <%= req === '/data' ? 'active' : '' %>">
                <a class="sidebar-link" href="/data">
                    <div class="nav-icon">
                        <i class="bi bi-bar-chart-fill"></i>
                    </div>
                    <span class="nav-text">Laporan</span>
                </a>
            </li>
            <li class="sidebar-item <%= req === '/settings' ? 'active' : '' %>">
                <a class="sidebar-link" href="/settings">
                    <div class="nav-icon">
                        <i class="bi bi-gear-fill"></i>
                    </div>
                    <span class="nav-text">Settings</span>
                </a>
            </li>
            <li class="sidebar-item <%= req === '/rbac-control' ? 'active' : '' %>">
                <a class="sidebar-link" href="/rbac-control">
                    <div class="nav-icon">
                        <i class="bi bi-shield-lock-fill"></i>
                    </div>
                    <span class="nav-text">RBAC Control</span>
                </a>
            </li>
        </ul>
        
        <div class="sidebar-footer">
            <div class="user-section">
                <div class="user-info-card">
                    <div class="user-avatar">
                        <i class="bi bi-person-circle"></i>
                    </div>
                    <div class="user-details">
                        <span id="user-info" class="user-name">Loading...</span>
                        <small class="user-role">Administrator</small>
                    </div>
                </div>
                <a class="logout-btn" href="/logout">
                    <i class="bi bi-box-arrow-right"></i>
                    <span class="logout-text">Logout</span>
                </a>
            </div>
        </div>
    </div>
</div>

<!-- Top Navbar -->
<nav class="top-navbar">
    <div class="navbar-content">
        <div class="navbar-left">
            <button class="sidebar-toggle-btn" id="sidebarToggle">
                <i class="bi bi-chevron-left"></i>
            </button>
            <button class="mobile-toggle-btn" id="mobileToggle">
                <i class="bi bi-list"></i>
            </button>
            <h5 class="page-title mb-0">
                <% 
                    const pageTitles = {
                        '/': 'Dashboard',
                        '/staff': 'Karyawan & Penilaian',
                        '/periode': 'Kategori Instrumen',
                        '/data': 'Laporan',
                        '/settings': 'Settings',
                        '/rbac-control': 'RBAC Control'
                    };
                    const currentTitle = pageTitles[req] || 'Dashboard';
                %>
                <%= currentTitle %>
            </h5>
        </div>
        <div class="navbar-right">
            <div class="time-display">
                <i class="bi bi-clock"></i>
                <span id="current-time-nav"></span>
            </div>
        </div>
    </div>
</nav>

<!-- Overlay for mobile -->
<div class="sidebar-overlay" id="sidebarOverlay"></div>

<style>
/* ========================================
   Clean White Modern Sidebar & Navbar
   ======================================== */

/* Early state - applied before DOMContentLoaded to prevent flash */
html.sidebar-minimized-early .fixed-sidebar {
    width: 72px;
}

html.sidebar-minimized-early .fixed-sidebar .logo-text,
html.sidebar-minimized-early .fixed-sidebar .nav-text,
html.sidebar-minimized-early .fixed-sidebar .user-details,
html.sidebar-minimized-early .fixed-sidebar .logout-text {
    opacity: 0;
    width: 0;
}

html.sidebar-minimized-early .fixed-sidebar .nav-icon,
html.sidebar-minimized-early .fixed-sidebar .user-avatar,
html.sidebar-minimized-early .fixed-sidebar .logout-btn i {
    margin-right: 0;
}

html.sidebar-minimized-early .fixed-sidebar .sidebar-link,
html.sidebar-minimized-early .fixed-sidebar .user-info-card {
    justify-content: center;
}

html.sidebar-minimized-early .top-navbar {
    left: 72px;
}

html.sidebar-minimized-early .sidebar-toggle-btn i {
    transform: rotate(180deg);
}

/* Fixed Sidebar - Clean White */
.fixed-sidebar {
    position: fixed;
    left: 0;
    top: 0;
    height: 100vh;
    width: 260px;
    background: #ffffff;
    border-right: 1px solid #e2e8f0;
    transition: all 0.3s ease;
    z-index: 1000;
    display: flex;
    flex-direction: column;
}

.fixed-sidebar.minimized {
    width: 72px;
}

/* Sidebar Header */
.sidebar-header {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 16px;
    border-bottom: 1px solid #e2e8f0;
    min-height: 64px;
    background: #ffffff;
}

.sidebar-logo {
    display: flex;
    align-items: center;
    overflow: hidden;
    justify-content: center;
}

.logo-img {
    width: 36px;
    height: 36px;
    border-radius: 8px;
    flex-shrink: 0;
}

.logo-text {
    font-weight: 700;
    font-size: 0.9rem;
    color: #1e293b;
    white-space: nowrap;
    letter-spacing: -0.025em;
    transition: opacity 0.3s ease;
    padding-left: 5px;
}

.fixed-sidebar.minimized .logo-text {
    opacity: 0;
    width: 0;
}

/* Sidebar Content */
.sidebar-content {
    flex: 1;
    overflow-y: auto;
    overflow-x: hidden;
    padding: 16px 0;
    display: flex;
    flex-direction: column;
}

.sidebar-content::-webkit-scrollbar {
    width: 4px;
}

.sidebar-content::-webkit-scrollbar-track {
    background: transparent;
}

.sidebar-content::-webkit-scrollbar-thumb {
    background: #cbd5e1;
    border-radius: 100px;
}

.sidebar-nav {
    list-style: none;
    padding: 0 12px;
    margin: 0;
    flex: 1;
}

.sidebar-item {
    margin: 4px 0;
}

.sidebar-link {
    display: flex;
    align-items: center;
    padding: 10px 12px;
    color: #64748b;
    text-decoration: none !important;
    border-radius: 8px;
    transition: all 0.2s ease;
    position: relative;
}

.sidebar-link:hover {
    background: #f1f5f9;
    color: #3b82f6 !important;
}

.sidebar-item.active .sidebar-link {
    background: #eff6ff;
    color: #3b82f6 !important;
}

.sidebar-item.active .sidebar-link::before {
    content: '';
    position: absolute;
    left: 0;
    top: 50%;
    transform: translateY(-50%);
    height: 24px;
    width: 3px;
    background: #3b82f6;
    border-radius: 0 3px 3px 0;
}

.nav-icon {
    width: 20px;
    height: 20px;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1.1rem;
    flex-shrink: 0;
    margin-right: 12px;
    transition: all 0.2s ease;
}

.sidebar-item.active .nav-icon {
    color: #3b82f6;
}

.nav-text {
    font-weight: 500;
    font-size: 0.875rem;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
    transition: opacity 0.3s ease;
    flex: 1;
}

.fixed-sidebar.minimized .nav-text {
    opacity: 0;
    width: 0;
}

.fixed-sidebar.minimized .nav-icon {
    margin-right: 0;
}

.fixed-sidebar.minimized .sidebar-link {
    justify-content: center;
}

/* Sidebar Footer */
.sidebar-footer {
    padding: 12px;
    border-top: 1px solid #e2e8f0;
}

.user-info-card {
    display: flex;
    align-items: center;
    margin-bottom: 10px;
    padding: 5px;
    background: #ffffff;
    border-radius: 8px;
    border: 1px solid #e2e8f0;
}

.fixed-sidebar.minimized .user-info-card {
    justify-content: center;
    padding: 8px;
}

.user-avatar {
    width: 32px;
    height: 32px;
    border-radius: 50%;
    background: #eff6ff;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 1rem;
    color: #3b82f6;
    flex-shrink: 0;
    margin-right: 10px;
}

.fixed-sidebar.minimized .user-avatar {
    margin-right: 0;
}

.user-details {
    flex: 1;
    overflow: hidden;
    transition: opacity 0.3s ease;
    min-width: 0;
}

.fixed-sidebar.minimized .user-details {
    opacity: 0;
    width: 0;
}

.user-name {
    display: block;
    color: #1e293b;
    font-weight: 600;
    font-size: 0.8rem;
    white-space: nowrap;
    overflow: hidden;
    text-overflow: ellipsis;
}

.user-role {
    color: #94a3b8;
    font-size: 0.7rem;
}

.logout-btn {
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 8px 12px;
    background: #fef2f2;
    color: #ef4444 !important;
    text-decoration: none !important;
    border-radius: 8px;
    transition: all 0.2s ease;
    font-weight: 500;
    font-size: 0.8rem;
}

.logout-btn:hover {
    background: #fee2e2;
    color: #dc2626 !important;
}

.logout-btn i {
    font-size: 1rem;
    flex-shrink: 0;
    margin-right: 8px;
}

.logout-text {
    transition: opacity 0.3s ease;
    white-space: nowrap;
}

.fixed-sidebar.minimized .logout-text {
    opacity: 0;
    width: 0;
}

.fixed-sidebar.minimized .logout-btn i {
    margin-right: 0;
}

/* Top Navbar - Clean White */
.top-navbar {
    position: fixed;
    top: 0;
    left: 260px;
    right: 0;
    height: 64px;
    background: #ffffff;
    border-bottom: 1px solid #e2e8f0;
    z-index: 999;
    transition: left 0.3s ease;
}

.fixed-sidebar.minimized ~ .top-navbar {
    left: 72px;
}

.navbar-content {
    display: flex;
    align-items: center;
    justify-content: space-between;
    height: 100%;
    padding: 0 24px;
}

.navbar-left {
    display: flex;
    align-items: center;
    gap: 16px;
}

.sidebar-toggle-btn {
    width: 36px;
    height: 36px;
    border-radius: 8px;
    background: #f1f5f9;
    border: 1px solid #e2e8f0;
    color: #64748b;
    display: flex;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    transition: all 0.2s ease;
    font-size: 1rem;
}

.sidebar-toggle-btn:hover {
    background: #e2e8f0;
    color: #3b82f6;
}

.sidebar-toggle-btn i {
    transition: transform 0.3s ease;
}

body.sidebar-minimized .sidebar-toggle-btn i {
    transform: rotate(180deg);
}

.mobile-toggle-btn {
    display: none;
    width: 36px;
    height: 36px;
    border-radius: 8px;
    background: #f1f5f9;
    border: 1px solid #e2e8f0;
    color: #64748b;
    align-items: center;
    justify-content: center;
    cursor: pointer;
    font-size: 1.25rem;
}

.mobile-toggle-btn:hover {
    background: #e2e8f0;
}

.page-title {
    color: #1e293b;
    font-weight: 600;
    font-size: 1rem;
}

.navbar-right {
    display: flex;
    align-items: center;
    gap: 12px;
}

.time-display {
    background: #f8fafc;
    padding: 8px 16px;
    border-radius: 8px;
    color: #64748b;
    font-size: 0.875rem;
    border: 1px solid #e2e8f0;
    display: flex;
    align-items: center;
    gap: 8px;
    font-weight: 500;
}

.time-display i {
    color: #3b82f6;
}

/* Sidebar Overlay for Mobile */
.sidebar-overlay {
    display: none;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: rgba(15, 23, 42, 0.5);
    z-index: 999;
    opacity: 0;
    transition: opacity 0.3s ease;
    backdrop-filter: blur(4px);
}

.sidebar-overlay.active {
    display: block;
    opacity: 1;
}

/* Responsive */
@media (max-width: 768px) {
    .fixed-sidebar {
        transform: translateX(-100%);
    }
    
    .fixed-sidebar.mobile-open {
        transform: translateX(0);
    }
    
    .top-navbar {
        left: 0;
    }
    
    .mobile-toggle-btn {
        display: flex;
    }
    
    .sidebar-toggle-btn {
        display: none;
    }
    
    .time-display {
        display: none;
    }
}

/* Animations */
@keyframes fadeIn {
    from {
        opacity: 0;
        transform: translateX(-8px);
    }
    to {
        opacity: 1;
        transform: translateX(0);
    }
}

.sidebar-item {
    animation: fadeIn 0.3s ease forwards;
}

.sidebar-item:nth-child(1) { animation-delay: 0.05s; }
.sidebar-item:nth-child(2) { animation-delay: 0.1s; }
.sidebar-item:nth-child(3) { animation-delay: 0.15s; }
.sidebar-item:nth-child(4) { animation-delay: 0.2s; }
.sidebar-item:nth-child(5) { animation-delay: 0.25s; }
.sidebar-item:nth-child(6) { animation-delay: 0.3s; }
</style>

<script>
// Sidebar Toggle Functionality
document.addEventListener('DOMContentLoaded', function() {
    const sidebar = document.getElementById('sidebar');
    const sidebarToggle = document.getElementById('sidebarToggle');
    const mobileToggle = document.getElementById('mobileToggle');
    const sidebarOverlay = document.getElementById('sidebarOverlay');
    
    // Sync state from early class to proper classes
    const savedState = localStorage.getItem('sidebarMinimized');
    if (savedState === 'true') {
        sidebar.classList.add('minimized');
        document.body.classList.add('sidebar-minimized');
    }
    // Remove early class after proper classes are applied
    document.documentElement.classList.remove('sidebar-minimized-early');
    
    // Desktop toggle
    if (sidebarToggle) {
        sidebarToggle.addEventListener('click', function() {
            sidebar.classList.toggle('minimized');
            document.body.classList.toggle('sidebar-minimized');
            
            // Save state to localStorage
            const isMinimized = sidebar.classList.contains('minimized');
            localStorage.setItem('sidebarMinimized', isMinimized);
        });
    }
    
    // Mobile toggle
    if (mobileToggle) {
        mobileToggle.addEventListener('click', function() {
            sidebar.classList.toggle('mobile-open');
            sidebarOverlay.classList.toggle('active');
        });
    }
    
    // Close sidebar when clicking overlay
    if (sidebarOverlay) {
        sidebarOverlay.addEventListener('click', function() {
            sidebar.classList.remove('mobile-open');
            sidebarOverlay.classList.remove('active');
        });
    }
    
    // Update time in navbar
    function updateTime() {
        const now = new Date();
        const timeString = now.toLocaleTimeString('id-ID', {
            hour: '2-digit',
            minute: '2-digit',
            second: '2-digit'
        });
        const timeElement = document.getElementById('current-time-nav');
        if (timeElement) {
            timeElement.textContent = timeString;
        }
    }
    
    updateTime();
    setInterval(updateTime, 1000);
    
    // Fetch user info
    fetch('/users/current-user')
        .then(res => res.json())
        .then(data => {
            const userInfoElement = document.getElementById('user-info');
            if (userInfoElement) {
                userInfoElement.textContent = data.email || data.name || 'User';
            }
        })
        .catch(err => {
            console.log('Error fetching user info:', err);
            const userInfoElement = document.getElementById('user-info');
            if (userInfoElement) {
                userInfoElement.textContent = 'User';
            }
        });
    
    // Close mobile sidebar when clicking a link
    const sidebarLinks = document.querySelectorAll('.sidebar-link');
    sidebarLinks.forEach(link => {
        link.addEventListener('click', function() {
            if (window.innerWidth <= 768) {
                sidebar.classList.remove('mobile-open');
                sidebarOverlay.classList.remove('active');
            }
        });
    });
});
</script>
